import { createAction } from '@lib/redux';

export const updateSettings = createAction('updateSettings');
export const getCountries = createAction('getCountries');

export const requestCurrency = createAction('requestCurrency');
